{
  "name": "sop-platform",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev:frontend": "pnpm --filter @sop/frontend dev",
    "dev:backend": "pnpm --filter @sop/backend dev",
    "build:frontend": "pnpm --filter @sop/frontend build",
    "build:backend": "pnpm --filter @sop/backend build",
    "db:generate": "pnpm --filter @sop/backend db:generate",
    "db:migrate": "pnpm --filter @sop/backend db:migrate",
    "db:seed": "pnpm --filter @sop/backend db:seed",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@trivago/prettier-plugin-sort-imports": "^6.0.2",
    "prettier": "^3.8.3",
    "prettier-plugin-tailwindcss": "^0.7.2",
    "typescript": "~6.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
